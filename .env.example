# このファイルを .env にコピーして値を埋める（.env はコミットされない）
# cp .env.example .env

# Gemini API（無料枠で可）。https://aistudio.google.com/apikey で取得
GEMINI_API_KEY=

# WordPress Application Password（投稿作成用）
# ユーザー → プロフィール → アプリケーションパスワード で発行
WP_USER=
WP_APP_PASS=
